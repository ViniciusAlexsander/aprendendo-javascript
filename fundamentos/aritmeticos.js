const [a,b,c,d] = [1,2,3,4]

const soma = a+b+c+d
const sub = a - b - c - d
const mult = a*b*c*d
const div = a/b/c/d
const modulo = a % 2

console.log("soma = " + soma)
console.log("sub = " + (sub))
console.log("mult = " + mult)
console.log("div = " + div)
